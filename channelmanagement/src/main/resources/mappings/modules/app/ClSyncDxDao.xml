<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jeeplus.modules.app.dao.ClSyncDxDao">

	<resultMap id="ClSyncDxResultMap" type="ClSyncDx">
		<result property="USR_CDE" column="USR_CDE" />
		<result property="IS_SYNC" column="IS_SYNC" />
		<result property="CRT_DT" column="CRT_DT" />
	</resultMap>
	
	<sql id="ClSyncDxColumns">
		a.USR_CDE,
		a.IS_SYNC,
		a.CRT_DT
	</sql>


<!-- 根据用户标识获得用户 对象-->
	<select id="cntByUsrCode" resultType="int" parameterType="java.lang.String">
		SELECT
		count(1) as count
		FROM CL_SYNC_DX a
		WHERE a.USR_CDE = #{USR_CDE}
	</select>
	
	<!-- 根据用户标识获得用户 对象-->
	<select id="getByUsrCode" resultMap="ClSyncDxResultMap" parameterType="java.lang.String">
		SELECT
		<include refid="ClSyncDxColumns" />
		FROM CL_SYNC_DX a
		WHERE a.USR_CDE = #{USR_CDE}
	</select>
	
	

	<!-- 插入用户 -->
	<insert id="insert">
		INSERT INTO CL_SYNC_DX(
		USR_CDE,
		IS_SYNC,
		CRT_DT
		) VALUES (
		#{USR_CDE},
		#{IS_SYNC},
		#{CRT_DT}
		)
	</insert>
	
	
	<!-- 插入用户 -->
	<update id="update">
		UPDATE CL_SYNC_DX SET
		IS_SYNC = #{IS_SYNC},
		CRT_DT = #{CRT_DT}
		WHERE USR_CDE = #{USR_CDE}
	</update>
	


</mapper>